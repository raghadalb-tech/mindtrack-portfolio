# MindTrack documentation snippets — example only (no real secrets)

# PostgreSQL (portfolio / production target)
DATABASE_URL=postgresql://mindtrack_app:changeme@localhost:5432/mindtrack_dev

# JWT (use a long random string in real deployments)
JWT_SECRET=replace-with-64-char-random-string

# LLM provider (never commit real keys)
LLM_API_KEY=your-provider-api-key-here
